<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="20dp">

        <!-- Texto superior añadido -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bienvenido a"
            android:textColor="#7A7A7A"
            android:textSize="20sp"
            android:layout_marginTop="20dp"
            />

        <!-- Logo de la hormiga -->
        <ImageView
            android:id="@+id/logoApp"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:contentDescription="Logo HandyMatch"
            android:src="@drawable/logo"
            android:layout_marginTop="10dp"/>

        <!-- Nombre de la app -->
        <ImageView
            android:id="@+id/nombreApp"
            android:layout_width="300dp"
            android:layout_height="80dp"
            android:contentDescription="HandyMatch"
            android:src="@drawable/letras"
            android:layout_marginTop="10dp"/>

        <!-- Texto descriptivo -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Conecta necesidades con soluciones"
            android:textColor="#5A5A5A"
            android:textSize="18sp"
            android:textAlignment="center"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="40dp"/>

        <!-- Botón principal -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnIniciar"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="250dp"
            android:layout_height="55dp"
            android:layout_gravity="center_horizontal"
            android:backgroundTint="@color/fondosyletras"
            android:onClick="abrirLogin"
            android:text="Comenzar ahora"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            app:cornerRadius="10dp" />

        <!-- Texto secundario -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Personas ayudando a personas"
            android:textColor="#7A7A7A"
            android:textSize="16sp"
            android:layout_marginTop="30dp"/>

    </LinearLayout>
</RelativeLayout>